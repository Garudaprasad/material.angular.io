import { NotificationManager } from './notification-manager';

describe('NotificationManager', () => {
  it('should create an instance', () => {
    expect(new NotificationManager()).toBeTruthy();
  });
});
